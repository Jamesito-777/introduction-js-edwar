// JAMES MACHADO HERNANDEZ
"use strict"
const numbers = [0, 12, 8, 9, 5, 70, 120];

// //Agregar elementos a mi arrgelo, pero con este metodo se requiere conocer la longitud del arreglo 
// numberslenght = numbers.length
// const console.log(numberslenght);

// numbers[numberslenght] = 100;


// console.log(numbers);

// Con el metodo push se agregan elementos al final del arreglo 
// numbers.push(700, 800);
// console.log(numbers);

// Metodos unshift para agregar elementos al inicio de un arreglo 
numbers.unshift(-3, -2, -1);


// Metodo para eliminar el ultimo elemento de mi arreglo 
// numbers.pop();

// JAMES MACHADO HERNANDEZ

// // Metodo shift para eliminar el primer elemento de mi arreglo
// numbers.shift();

// Metodo splice elimina todos los elementos a partir del indice asignado.

numbers.splice(5);


numbers.indexOf(0);
console.table(numbers);
